<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= client.name %> — ทางเข้าสู่ระบบ</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <header>
      <h1><%= client.name %></h1>
      <p><a href="/">กลับไปหน้าหลัก</a></p>
    </header>
    <main>
      <section>
        <p><%= client.description %></p>
        <p>ลิงก์ไปยังระบบลูกค้า:</p>
        <p><a class="btn" href="<%= client.url %>" target="_blank" rel="noopener noreferrer">เปิดระบบของ <%= client.name %></a></p>
      </section>
    </main>
    <footer>
      <p>© Han</p>
    </footer>
  </body>
</html>
